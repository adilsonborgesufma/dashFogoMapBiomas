# Passo 1
del .gitignore

# Passo 3: Apagar o repositório Git interno da pasta dashmapbiomas_mark1
Remove-Item -Recurse -Force dashmapbiomas_mark1\.git

# Passo 3: Verifique se a pasta é um submódulo
git submodule status

# Passo 4: Confirme que o repositório remoto está configurado:
git remote -v

Deve aparecer algo assim:
origin  https://github.com/adilsonborgesufma/dashmapbiomas_mark1.git (fetch)
origin  https://github.com/adilsonborgesufma/dashmapbiomas_mark1.git (push)

# Passo 5: Adicione todos os arquivos:
git add .

# Passo 6:
git commit -m "Deploy MapBiomas"

# Passo 7:
git branch

# Passo 8: 
git pull origin main --allow-unrelated-histories

# Passo 9: 
git push origin main